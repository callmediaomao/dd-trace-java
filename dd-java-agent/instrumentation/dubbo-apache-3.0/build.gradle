apply from: "$rootDir/gradle/java.gradle"

muzzle {
  pass {
    group = "org.apache.dubbo"
    module = "dubbo"
    versions = "[3.0.0,)"
  }
}


dependencies {
  compileOnly group: 'org.apache.dubbo', name: 'dubbo', version: '3.0.0'
}

